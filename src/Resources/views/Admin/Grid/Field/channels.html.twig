{% if data|length %}
    <ul>
        {% for channel in data %}
            <li>
                {{ channel.name }}
            </li>
        {% endfor %}
    </ul>
{% else %}
    <i class="exclamation circle icon red"></i> {{ 'gabrielcol_sylius_rec_ai.ui.no_channels'|trans }}
{% endif %}
